package com.example.diycode.utils;

import android.content.Context;
import android.util.LruCache;

import com.mystudy.diycode_api.utils.ACache;
import com.mystudy.diycode_api.utils.FileUtil;

import java.io.File;

/**
 * 缓存工具
 */
public class DataCache {
//    private static int M = 1024 * 1024;
//    ACache mDiskCache;
//    LruCache<String, Object> mLruCache;
//    public DataCache(Context context) {
//        mDiskCache = ACache.get(new File(FileUtil.getExternalCacheDir(context.getApplicationContext(), "diy-data")));
//        mLruCache = new LruCache<>(5 * M);
//    }
}
